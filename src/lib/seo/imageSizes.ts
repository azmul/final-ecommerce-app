/** Responsive `sizes` for product cards in grids (~2–4 columns). */
export const PRODUCT_CARD_IMAGE_SIZES =
  '(max-width: 640px) 50vw, (max-width: 1024px) 33vw, (max-width: 1280px) 25vw, 280px'

/** Hero / large tile images (e.g. three-item grid full column). */
export const PRODUCT_HERO_IMAGE_SIZES = '(max-width: 768px) 100vw, (max-width: 1280px) 50vw, 640px'

/** Small gallery thumbnails. */
export const GALLERY_THUMB_IMAGE_SIZES = '80px'
