/** Brand gold used across the customer chat widget. */
export const CHAT_THEME_COLOR = '#d4a017'

/** Lighter gold for inset rings and highlights. */
export const CHAT_THEME_RING_INNER = '#f0cc4a'

/** Scoped theme class — gold accent for the floating launcher button only. */
export const CHAT_THEME_CLASS = 'chat-theme'

/** Gold gradient surface shared with header cart / wishlist controls. */
export const CHAT_LAUNCHER_SURFACE_CLASS =
  'bg-gradient-to-br from-primary to-primary/85 text-white shadow-md shadow-primary/25 ring-2 ring-inset ring-[color:var(--chat-ring-inner)]'

/** Red count badge (matches chat unread indicator). */
export const CHAT_UNREAD_BADGE_CLASS =
  'rounded-full bg-destructive font-bold text-white shadow-md'
