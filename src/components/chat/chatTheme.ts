/** Brand gold used across the customer chat widget. */
export const CHAT_THEME_COLOR = '#d4a017'

/** Lighter gold for inset rings and highlights. */
export const CHAT_THEME_RING_INNER = '#f0cc4a'

/** Scoped theme class — gold accent for the floating launcher button only. */
export const CHAT_THEME_CLASS = 'chat-theme'
